package app;

import java.util.stream.IntStream;

public class SumNaturale {

    public static int Sum(int number) {
        IntStream intStream = IntStream.range(0, number);
        intStream.forEach(action);

        return 0;
    }

    public static void main(String[] args) {

    }
}